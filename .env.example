MONGODB_URI=

# Supabase (server): used to push order realtime patches via Realtime broadcast REST.
# Never commit real keys. Copy to .env locally.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Optional (default 4000)
# PORT=4000
